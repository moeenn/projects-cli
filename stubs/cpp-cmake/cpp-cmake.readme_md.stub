{{ define "cpp-cmake.readme_md" }}
## {{ .ProjectName }}

#### Build from source

```bash
$ mkdir build
$ cd ./build
$ cmake .. -D CMAKE_BUILD_TYPE=Debug && make
```

**Note**: The compiled binary will be placed in `./build/` directory.
{{ end }}
